# 前后端全量 BUG 排查报告

> 日期：2026-07-11（二次全量复扫）  
> 范围：OVH_WEBUI 前后端、只读实机烟测、静态检查、契约与关键路径代码审查

---

## 1. 检查手段与结论总览

| 检查 | 结果 |
|------|------|
| `go vet ./...` | 通过 |
| `go test ./internal/...` | catalog / numconv 通过 |
| `go build` | 通过 |
| 前端 `tsc -b` | 通过 |
| 实机全功能烟测 `full_functional_test.py` | **54/54 PASS** |
| 精简烟测 `smoke_test.py` | 已对齐下线功能后 **PASS** |
| 后端健康 | `http://127.0.0.1:19998` 运行中 |
| 前端 + Vite 代理 | `http://127.0.0.1:8080`，`/health` `/api/*` 代理正常 |

**结论：主链路可用；未发现阻断级运行时 BUG。历史 P0/P1 契约问题仍保持修复态；本次修复测试脚本漂移与部分错误提示字段；另有非阻塞债务。**

---

## 2. 已确认并已修复的 BUG（历史 + 本次）

### P0 / P1（影响功能正确性）— 此前已修，复扫仍正确

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| B1 | 联系人列表永远为空：后端 `{status, data}` vs 前端 `requests` | `api.ts` | ✅ 兼容 |
| B2 | 账单/列表包装字段不一致 | `wrapList` / `asArray` | ✅ |
| B3 | accept/refuse 只认 `success`，后端回 `status` | `normalizeActionResult` | ✅ |
| B4 | accept/refuse 缺 token → 400 | ContactChangeDialog 正式 Dialog + ContactChangePage prompt | ✅ |
| B5 | 顶栏「后端离线」：错误 `backendUrl` 误报 | `useBackendConnection` 回退 `/health` | ✅ |
| B6 | 存储 SAS 被标成 SA | `storage_parse.go` sas\|ssd\|nvme\|hdd\|sa | ✅ 复扫 0 误标 |
| B7 | 服务器列表空：缺 `showApiServers=true` | FE 默认带参 | ✅ |

### 本次复扫新增修复

| # | 问题 | 级别 | 处理 |
|---|------|------|------|
| B8 | `smoke_test.py` 仍调用已下线 `/api/inspection/*` → 假 FAIL | 中（测试） | 改为断言 404 + catalog/hardware 只读 |
| B9 | 加入监控失败时 toast 只读 `error`，后端用 `message`（如 TG 未配置） | 低 | `use-servers` / `use-settings` / `use-vps-monitor` 优先 `message` |
| B10 | `types.MonitorStatus` camelCase 与后端 snake_case 不一致 | 低 | 类型对齐 snake_case |

---

## 3. 实机契约抽检（关键 API）

| 接口 | HTTP | 形态 | 前端兼容 |
|------|------|------|----------|
| `/health` | 200 | `{status,time}` | OK |
| `/api/stats` | 200 | camelCase 业务字段 | OK |
| `/api/monitor/status` | 200 | snake_case | MonitorPage / VpsMonitor 用 snake |
| `/api/monitor/subscriptions` | 200 | 数组 | OK |
| `/api/servers?showApiServers=true` | 200 | `{servers, cacheInfo}` | OK，storage 抽样正确 |
| `/api/accounts` | 200 | `{accounts,total}` | OK |
| `/api/ovh/account/orders` | 200 | **裸数组** | wrapList OK |
| `/api/ovh/account/bills` | 200 | `{status,data}` | wrapList OK |
| `/api/ovh/contact-change-requests` | 200 | `{status,data}` | OK |
| `/api/server-control/list` | 200 | `{success,servers,total}` + `serviceName` | OK |
| `/api/server-control/:s/hardware` | 200 | `{hardware}` | OK |
| `/api/server-control/:s/serviceinfo` | 200 | `{serviceInfo}` | OK |
| `/api/vps-control/list` | 200 | total=0（账户无 VPS） | 数据现状 |
| `/api/inspection/*` | 404 | 已下线 | 符合 ADR |
| burst / firewall / backup-ftp | 404 | SKU 未开通 + `notAvailable` | FE 已 catch 404 |

---

## 4. 仍存在但非阻塞 / 环境相关

| 级别 | 问题 | 说明 |
|------|------|------|
| ~~中~~ | ~~双 HTTP 客户端~~ | ✅ 已统一为 `lib/http.ts` axios；`api.ts` 仅 facade；支持 backendUrl |
| ~~中~~ | ~~文档漂移 inspection~~ | ✅ README / handover / package.json 已清理 |
| 低 | ContactChangePage 仍用 `window.prompt` | ServerControl 内 Dialog 已正规化；独立页体验较弱 |
| 低 | 部分独服 soft 失败 | statistics 500、console 500、bios/backup-ftp 404：机型/未开通，非逻辑错误 |
| 低 | handlers 单测覆盖少 | 多数 package 无 test |
| 运维 | 后端需常驻进程 | 后台挂起 `ovh-webui.exe` 较稳 |

---

## 5. 未发现的问题（排除项）

- 独服控制 `serviceName` / `hardware` / `serviceInfo` 包装层前后端一致  
- 路由无死链到已删 Inspection 页  
- 存储解析：SAS/SATA/NVMe 抽样无 SA 误标；hybrid 显示「混合RAID」  
- 全功能只读 54/54 覆盖：账户、目录、目标机只读详情、询价、监控 PUT 404 契约  
- TypeScript / go vet / unit tests 全绿  

---

## 6. 建议后续（非本次必改）

1. ContactChangePage 改用与 ChangeContactDialog 相同的 Token Dialog  
2. 补 handlers 集成测试  
3. Windows 服务/计划任务托管后端  
4. CI 流水线  

---

## 7. 本次排查代码改动

- `scripts/smoke_test.py` — 对齐巡检下线  
- `src/hooks/ovh/use-servers.ts` — 监控错误 message  
- `src/hooks/ovh/use-settings.ts` — 设置错误 message  
- `src/hooks/ovh/use-vps-monitor.ts` — VPS 订阅错误 message  
- `src/lib/types.ts` — MonitorStatus 契约  

---

## 8. 复扫一句话

**前后端当前无阻断级 BUG；核心业务经 54/54 实机只读验证；测试脚本与错误提示已对齐后端契约；剩余为架构债务与文档清理。**
